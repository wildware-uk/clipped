# tests/audio

System tests that record known tone generators and assert that audio sources
which are meant to stay separate really are separate (AGENTS.md section 21).

These tests depend on audio hardware and endpoint configuration, so they are not
part of the pull-request CI job. Populated by the M2 milestone.
