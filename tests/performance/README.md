# tests/performance

Benchmarks and soak tests measuring the resource targets in SPEC.md section 38.

These are long-running and hardware-dependent, so they are not part of the
pull-request CI job. Populated by the M14 milestone.
