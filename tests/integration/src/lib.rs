//! Cross-crate tests that assert workspace-wide invariants.
//!
//! This package intentionally ships no library code. It exists so that tests
//! which are about the workspace as a whole, rather than about any single
//! crate, have somewhere to live. See the `tests/` directory alongside this
//! file.
