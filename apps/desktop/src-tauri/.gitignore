# Cargo's output for this crate. The repository's root .gitignore anchors its
# `/target/` pattern to the repository root, so this directory needs its own.
/target/

# Generated by `tauri-build` on every build from tauri.conf.json and the files
# under capabilities/: the permission schemas the capability files validate
# against. Nothing here is written by hand.
/gen/
