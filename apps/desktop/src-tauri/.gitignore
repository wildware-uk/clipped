# This crate is its own Cargo workspace (see Cargo.toml), so its build output
# lands here rather than in the repository's /target.
/target/

# Written by tauri-build from tauri.conf.json on every build.
/gen/schemas/
