// `toBeVisible`, `toHaveAccessibleName` and the rest of the DOM matchers. They
// are what lets an accessibility assertion read as one, rather than as a walk
// over attributes.
import '@testing-library/jest-dom/vitest';
