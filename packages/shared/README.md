# packages/shared

TypeScript types and helpers shared between `apps/desktop` and `packages/ui`,
including the types mirroring the recorder IPC protocol.

This directory is a placeholder. The package is created alongside the desktop
application scaffold in the M5 milestone.
